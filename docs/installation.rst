============
Installation
============

At the command line::

    $ easy_install django-meetup-wrapper

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-meetup-wrapper
    $ pip install django-meetup-wrapper
